Per installare:

apt-get install libhunspell-dev
pip install spacy_hunspell

Firma:

BioNLP(biospacy, rules, dysplasia_mappings, dict_path, aff_path)

dict_path: serve il file path per en_US.dic 

aff_path: serve il file path per en_US.aff


Metodo: 

spell_checker(text)

text: è di tipo str (string)